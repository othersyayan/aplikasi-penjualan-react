service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {                                   // toko/{uid}/produk/{filename}
      allow read, write: if request.auth!=null;
      // allow read, write: if request.auth.uid==uid;
    }
  }
}
